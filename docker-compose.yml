services:
  frontend:
    image: ghcr.io/ilvi-platform/symptex-frontend:${SYMTEX_API_TAG:-latest}
    build:
      context: ./frontend
    ports:
      - "8501:8501"
    restart: always

  symptex:
    image: ghcr.io/ilvi-platform/symptex-api:${SYMTEX_API_TAG:-latest}
    build:
      context: ./api
    ports:
      - "8000:8000"
    restart: always
    environment:
      - DATABASE_URL=postgresql://ilvi:ilvi@postgres:5432/ilvi
    networks:
      - ilvi-api_default

networks:
  ilvi-api_default:
    external: true